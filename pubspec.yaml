name: datamites
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.8+8

environment:
  sdk: '>=2.18.6 <3.0.0'
#  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.18.0
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.13
  http: ^0.13.6
  carousel_slider: ^4.1.1
  flutter_html: ^3.0.0-beta.2
  share: ^2.0.4  # package is discontinued
  expandable: ^5.0.1
  sembast: ^3.1.0+2
  path_provider: ^2.0.2
  pull_to_refresh: ^2.0.0
  url_launcher: ^6.1.2
  flutter_phoenix: ^1.0.0
  image_picker: ^1.0.4
  country_code_picker: ^3.0.0
  firebase_remote_config: ^4.2.7
  firebase_core: ^2.17.0
  firebase_messaging: ^14.2.2
  flutter_local_notifications: ^16.1.0
  qr_code_scanner: ^1.0.1
  flutter_layout_grid: ^2.0.5 #added this for resolving issue
  flutter_math_fork: ^0.6.0 #added this for resolving issue
  webview_flutter: ^2.3.0 #added this for resolving issue
  firebase_auth: ^4.10.1 #added this
  cloud_firestore: ^4.9.3 #added this
  flutter_pdfview: ^1.3.2 #added this
  permission_handler: ^10.2.0

dev_dependencies:
  change_app_package_name: ^1.1.0
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/skillogic.png
    - assets/skillogic_icon.png
